spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/project-healy?createDataBaseIfNotExist=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

# swagger-ui custom path
  springdoc:
    swagger-ui:
      path: /swagger-ui.html
